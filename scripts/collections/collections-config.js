/**
 * Collections Configuration Constants
 * Centralized configuration for the collections module
 */

/**
 * Debug flag for collections module
 * Set to true to enable verbose logging
 */
export const DEBUG_COLLECTIONS = true;

/**
 * Default Dynamic Media bucket if not set in APP_CONFIG
 */
export const DEFAULT_BUCKET = 'delivery-p64403-e544653';

/**
 * API Configuration
 * API keys for staging and production environments
 */
export const API_CONFIG = {
  stagingApiKey: 'polaris-asset-search-api-key',
  productionApiKey: 'aem-assets-content-hub-1',
};
